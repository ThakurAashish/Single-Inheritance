# Single-Inheritance
Single inheritance is a type of inheritance in Object-Oriented Programming (OOP) where a child class (derived class) inherits properties and behaviors (methods and attributes) from only one parent class (base class).  It helps in code reusability and avoiding redundancy.
